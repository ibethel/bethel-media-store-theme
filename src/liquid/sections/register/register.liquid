{%- render 'register' -%}

{% schema %}
  {
    "name": "t:sections.register.name",
    "settings": [
      {
        "type": "header",
        "content": "t:sections.register.name"
      }
    ]
  }
{% endschema %}
