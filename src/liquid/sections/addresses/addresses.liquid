{%- render 'addresses' -%}

{% schema %}
  {
    "name": "t:sections.addresses.name",
    "settings": [
      {
        "type": "header",
        "content": "t:sections.addresses.name"
      }
    ]
  }
{% endschema %}
