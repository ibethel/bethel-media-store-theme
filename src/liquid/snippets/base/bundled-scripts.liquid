{%- capture bundle_script_name -%}
  bundle.{{- layout -}}.js
{%- endcapture -%}

{{ bundle_script_name | asset_url | script_tag }}

{%- if template != blank -%}
  {%- capture bundle_template_script_name -%}
    bundle.{{- template.name -}}

    {%- if template.suffix != blank -%}
      .{{- template.suffix -}}
    {%- endif -%}

    .js
  {%- endcapture -%}

  {{ bundle_template_script_name | asset_url | script_tag }}

{%- endif -%}
