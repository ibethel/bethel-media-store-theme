<div
  class="accordion-item border-bottom border-color-grey-10 border-1

    {%- if first_item %} border-top{%- endif -%}"
>

  {%- capture block_title_content -%}
    <button
      class="accordion-item__title-button bm-btn bm-btn--no-styles d-flex align-items-center justify-content-between col-12 px-0 py-3 closed-accordion"
      type="button"
      data-target="{{- data_target -}}"
      aria-controls="{{- data_target -}}"
      aria-expanded="false"
    >

      {%- render 'bm-title-block',
        enable_title_styles: enable_title_styles,
        title_classes: "mb-0",
        title_color: title_color,
        title_content: title_content,
        title_font: title_font,
        title_tag: "text",
        title_italic: title_italic,
        title_size: title_size,
        title_size_tablet: title_size_tablet,
        title_size_desk: title_size_desk,
        title_weight: title_weight -%}

      {%- render 'bm-icon-template',
        bm_icon: "icon-chevron-up",
        bm_icon_classes: "d-block",
        icon_classes: "d-block" -%}

    </button>
  {%- endcapture -%}

  {%- render 'bm-title-block',
    title_classes: "mb-0",
    title_content: block_title_content,
    title_tag: title_tag,
    title_labelledby: title_labelledby -%}

  <div
    id="{{- data_target -}}"
    class="accordion-item__content close-accordion"
    aria-labelledby="{{- title_labelledby -}}"
    aria-hidden="true"
  >

    {%- render 'bm-message-block',
      enable_message_styles: enable_message_styles,
      message_alignment_desk: message_alignment_desk,
      message_alignment_tablet: message_alignment_tablet,
      message_alignment_phone: message_alignment_phone,
      message_classes: "mb-0 pb-3",
      message_color: message_color,
      message_content: message_content,
      message_font: message_font,
      message_has_html: message_has_html,
      message_italic: message_italic,
      message_line_height: message_line_height,
      message_size: message_size,
      message_size_tablet: message_size_tablet,
      message_size_desk: message_size_desk,
      message_weight: message_weight -%}

  </div>
</div>
