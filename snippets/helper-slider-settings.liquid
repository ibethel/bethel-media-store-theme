{
  "arrows": {{- arrows | json -}},
  "autoplay": {{- autoplay | json -}},
  "autoScroll": {{- autoscroll | json -}},
  "breakpoints": {
    "desk": {
      "arrows": {{- arrows_desk | json -}},
      "padding": {
        "right": {{- padding_right_desk | json -}}
      },
      "perPage": {{- per_page_desk | json -}}
    },
    "phone": {
      "arrows": {{- arrows_phone | json -}},
      "padding": {
        "right": {{- padding_right_phone | json -}}
      },
      "perPage": {{- per_page_phone | json -}}
    },
    "tablet": {
      "arrows": {{- arrows_tablet | json -}},
      "padding": {
        "right": {{- padding_right_tablet | json -}}
      },
      "perPage": {{- per_page_tablet | json -}}
    }
  },
  "padding": {
    "right": {{- padding_right_max | json -}}
  },
  "changeVariantImage": {{- change_variant_image | json -}},
  "clones": {{- clones | json -}},
  "gap": {{- gap | json -}},
  "isNavigation": {{- is_navigation | json -}},
  "pagination": {{- pagination | json -}},
  "perMove": {{- per_move_max | json -}},
  "perPage": {{- per_page_max | json -}},
  "primaryThumbnail": {{- primary_thumbnail | json -}},
  "rewind": {{- rewind | json -}},
  "secondaryThumbnail": {{- secondary_thumbnail | json -}},
  "speed": {{- speed | json -}},
  "interval": {{- interval | json -}}
}
