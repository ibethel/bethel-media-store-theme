{%- render 'order' -%}

{% schema %}
  {
    "name": "t:sections.order.name",
    "settings": [
      {
        "type": "header",
        "content": "t:sections.order.name"
      }
    ]
  }
{% endschema %}
