{%- render "404",
  menu: section.settings.menu -%}


{% schema %}
  {
    "name": "t:sections.404.name",
    "settings": [
      {
        "type": "link_list",
        "id": "menu",
        "label": "t:sections.404.settings.menu.label"
      }
    ]
  }
{% endschema %}
