{%- render 'activate-account' -%}

{% schema %}
  {
    "name": "t:sections.activate-account.name",
    "settings": [
      {
        "type": "header",
        "content": "t:sections.activate-account.name"
      }
    ]
  }
{% endschema %}
