{%- render 'password-page' -%}

{% schema %}
  {
    "name": "t:sections.password.name",
    "settings": [
      {
        "type": "header",
        "content": "t:sections.password.name"
      }
    ]
  }
{% endschema %}
